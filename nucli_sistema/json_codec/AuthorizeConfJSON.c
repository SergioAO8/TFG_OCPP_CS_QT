/**
 * stdout
 * This file has been autogenerated using quicktype https://github.com/quicktype/quicktype - DO NOT EDIT
 * This file depends of https://github.com/DaveGamble/cJSON, https://github.com/joelguittet/c-list and https://github.com/joelguittet/c-hashtable
 * To parse json data from json string use the following: struct <type> * data = cJSON_Parse<type>(<string>);
 * To get json data from cJSON object use the following: struct <type> * data = cJSON_Get<type>Value(<cjson>);
 * To get cJSON object from json data use the following: cJSON * cjson = cJSON_Create<type>(<data>);
 * To print json string from json data use the following: char * string = cJSON_Print<type>(<data>);
 * To delete json data use the following: cJSON_Delete<type>(<data>);
 */

#ifndef __STDOUT__
#define __STDOUT__

#ifdef __cplusplus
extern "C" {
#endif

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#include <string.h>
#include <cJSON.h>
#include <hashtable.h>
#include <list.h>
#include "AuthorizeConfJSON.h"
#include "mystrdup.h"

#ifndef cJSON_Bool
#define cJSON_Bool (cJSON_True | cJSON_False)
#endif
#ifndef cJSON_Map
#define cJSON_Map (1 << 16)
#endif
#ifndef cJSON_Enum
#define cJSON_Enum (1 << 17)
#endif

static enum Status cJSON_GetStatusValue(const cJSON * j);
static cJSON * cJSON_CreateStatus(const enum Status x);

static struct IdTagInfo * cJSON_ParseIdTagInfo(const char * s);
static struct IdTagInfo * cJSON_GetIdTagInfoValue(const cJSON * j);
static cJSON * cJSON_CreateIdTagInfo(const struct IdTagInfo * x);
static char * cJSON_PrintIdTagInfo(const struct IdTagInfo * x);
static void cJSON_DeleteIdTagInfo(struct IdTagInfo * x);


static struct AuthorizeConf * cJSON_GetAuthorizeConfValue(const cJSON * j);
static cJSON * cJSON_CreateAuthorizeConf(const struct AuthorizeConf * x);
static void cJSON_DeleteAuthorizeConf(struct AuthorizeConf * x);

enum Status cJSON_GetStatusValue(const cJSON * j) {
    enum Status x = 0;
    if (NULL != j) {
        if (!strcmp(cJSON_GetStringValue(j), "Accepted")) x = STATUS_ACCEPTED;
        else if (!strcmp(cJSON_GetStringValue(j), "Blocked")) x = STATUS_BLOCKED;
        else if (!strcmp(cJSON_GetStringValue(j), "ConcurrentTx")) x = STATUS_CONCURRENT_TX;
        else if (!strcmp(cJSON_GetStringValue(j), "Expired")) x = STATUS_EXPIRED;
        else if (!strcmp(cJSON_GetStringValue(j), "Invalid")) x = STATUS_INVALID;
    }
    return x;
}

static cJSON * cJSON_CreateStatus(const enum Status x) {
    cJSON * j = NULL;
    switch (x) {
        case STATUS_ACCEPTED: j = cJSON_CreateString("Accepted"); break;
        case STATUS_BLOCKED: j = cJSON_CreateString("Blocked"); break;
        case STATUS_CONCURRENT_TX: j = cJSON_CreateString("ConcurrentTx"); break;
        case STATUS_EXPIRED: j = cJSON_CreateString("Expired"); break;
        case STATUS_INVALID: j = cJSON_CreateString("Invalid"); break;
    }
    return j;
}

static struct IdTagInfo * cJSON_ParseIdTagInfo(const char * s) {
    struct IdTagInfo * x = NULL;
    if (NULL != s) {
        cJSON * j = cJSON_Parse(s);
        if (NULL != j) {
            x = cJSON_GetIdTagInfoValue(j);
            cJSON_Delete(j);
        }
    }
    return x;
}

static struct IdTagInfo * cJSON_GetIdTagInfoValue(const cJSON * j) {
    struct IdTagInfo * x = NULL;
    if (NULL != j) {
        if (NULL != (x = cJSON_malloc(sizeof(struct IdTagInfo)))) {
            memset(x, 0, sizeof(struct IdTagInfo));
            if (cJSON_HasObjectItem(j, "expiryDate")) {
                x->expiry_date = mystrdup(cJSON_GetStringValue(cJSON_GetObjectItemCaseSensitive(j, "expiryDate")));
            }
            if (cJSON_HasObjectItem(j, "parentIdTag")) {
                x->parent_id_tag = mystrdup(cJSON_GetStringValue(cJSON_GetObjectItemCaseSensitive(j, "parentIdTag")));
            }
            if (cJSON_HasObjectItem(j, "status")) {
                x->status = cJSON_GetStatusValue(cJSON_GetObjectItemCaseSensitive(j, "status"));
            }
        }
    }
    return x;
}

static cJSON * cJSON_CreateIdTagInfo(const struct IdTagInfo * x) {
    cJSON * j = NULL;
    if (NULL != x) {
        if (NULL != (j = cJSON_CreateObject())) {
            if (NULL != x->expiry_date) {
                cJSON_AddStringToObject(j, "expiryDate", x->expiry_date);
            }
            if (NULL != x->parent_id_tag) {
                cJSON_AddStringToObject(j, "parentIdTag", x->parent_id_tag);
            }
            cJSON_AddItemToObject(j, "status", cJSON_CreateStatus(x->status));
        }
    }
    return j;
}

static char * cJSON_PrintIdTagInfo(const struct IdTagInfo * x) {
    char * s = NULL;
    if (NULL != x) {
        cJSON * j = cJSON_CreateIdTagInfo(x);
        if (NULL != j) {
            s = cJSON_Print(j);
            cJSON_Delete(j);
        }
    }
    return s;
}

static void cJSON_DeleteIdTagInfo(struct IdTagInfo * x) {
    if (NULL != x) {
        if (NULL != x->expiry_date) {
            cJSON_free(x->expiry_date);
        }
        if (NULL != x->parent_id_tag) {
            cJSON_free(x->parent_id_tag);
        }
        cJSON_free(x);
    }
}

struct AuthorizeConf * cJSON_ParseAuthorizeConf(const char * s) {
    struct AuthorizeConf * x = NULL;
    if (NULL != s) {
        cJSON * j = cJSON_Parse(s);
        if (NULL != j) {
            x = cJSON_GetAuthorizeConfValue(j);
            cJSON_Delete(j);
        }
    }
    return x;
}

static struct AuthorizeConf * cJSON_GetAuthorizeConfValue(const cJSON * j) {
    struct AuthorizeConf * x = NULL;
    if (NULL != j) {
        if (NULL != (x = cJSON_malloc(sizeof(struct AuthorizeConf)))) {
            memset(x, 0, sizeof(struct AuthorizeConf));
            if (cJSON_HasObjectItem(j, "idTagInfo")) {
                x->id_tag_info = cJSON_GetIdTagInfoValue(cJSON_GetObjectItemCaseSensitive(j, "idTagInfo"));
            }
        }
    }
    return x;
}

static cJSON * cJSON_CreateAuthorizeConf(const struct AuthorizeConf * x) {
    cJSON * j = NULL;
    if (NULL != x) {
        if (NULL != (j = cJSON_CreateObject())) {
            cJSON_AddItemToObject(j, "idTagInfo", cJSON_CreateIdTagInfo(x->id_tag_info));
        }
    }
    return j;
}

char * cJSON_PrintAuthorizeConf(const struct AuthorizeConf * x) {
    char * s = NULL;
    if (NULL != x) {
        cJSON * j = cJSON_CreateAuthorizeConf(x);
        if (NULL != j) {
            s = cJSON_Print(j);
            cJSON_Delete(j);
        }
    }
    return s;
}

static void cJSON_DeleteAuthorizeConf(struct AuthorizeConf * x) {
    if (NULL != x) {
        if (NULL != x->id_tag_info) {
            cJSON_DeleteIdTagInfo(x->id_tag_info);
        }
        cJSON_free(x);
    }
}

#ifdef __cplusplus
}
#endif

#endif /* __STDOUT__ */
