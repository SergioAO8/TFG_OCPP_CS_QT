/**
 * stdout
 * This file has been autogenerated using quicktype https://github.com/quicktype/quicktype - DO NOT EDIT
 * This file depends of https://github.com/DaveGamble/cJSON, https://github.com/joelguittet/c-list and https://github.com/joelguittet/c-hashtable
 * To parse json data from json string use the following: struct <type> * data = cJSON_Parse<type>(<string>);
 * To get json data from cJSON object use the following: struct <type> * data = cJSON_Get<type>Value(<cjson>);
 * To get cJSON object from json data use the following: cJSON * cjson = cJSON_Create<type>(<data>);
 * To print json string from json data use the following: char * string = cJSON_Print<type>(<data>);
 * To delete json data use the following: cJSON_Delete<type>(<data>);
 */

#ifndef __STDOUT__
#define __STDOUT__

#ifdef __cplusplus
extern "C" {
#endif

#include <stdint.h>
#include <stdbool.h>
#include <stdlib.h>
#include <string.h>
#include <cJSON.h>
#include <hashtable.h>
#include <list.h>
#include "RemoteStopTransactionReqJSON.h"
#include "mystrdup.h"

#ifndef cJSON_Bool
#define cJSON_Bool (cJSON_True | cJSON_False)
#endif
#ifndef cJSON_Map
#define cJSON_Map (1 << 16)
#endif
#ifndef cJSON_Enum
#define cJSON_Enum (1 << 17)
#endif

static struct RemoteStopTransactionReq * cJSON_GetRemoteStopTransactionReqValue(const cJSON * j);
static cJSON * cJSON_CreateRemoteStopTransactionReq(const struct RemoteStopTransactionReq * x);
static void cJSON_DeleteRemoteStopTransactionReq(struct RemoteStopTransactionReq * x);

struct RemoteStopTransactionReq * cJSON_ParseRemoteStopTransactionReq(const char * s) {
    struct RemoteStopTransactionReq * x = NULL;
    if (NULL != s) {
        cJSON * j = cJSON_Parse(s);
        if (NULL != j) {
            x = cJSON_GetRemoteStopTransactionReqValue(j);
            cJSON_Delete(j);
        }
    }
    return x;
}

static struct RemoteStopTransactionReq * cJSON_GetRemoteStopTransactionReqValue(const cJSON * j) {
    struct RemoteStopTransactionReq * x = NULL;
    if (NULL != j) {
        if (NULL != (x = cJSON_malloc(sizeof(struct RemoteStopTransactionReq)))) {
            memset(x, 0, sizeof(struct RemoteStopTransactionReq));
            if (cJSON_HasObjectItem(j, "transactionId")) {
                x->transaction_id = cJSON_GetNumberValue(cJSON_GetObjectItemCaseSensitive(j, "transactionId"));
            }
            else
                x->transaction_id = -1;
        }
    }
    return x;
}

static cJSON * cJSON_CreateRemoteStopTransactionReq(const struct RemoteStopTransactionReq * x) {
    cJSON * j = NULL;
    if (NULL != x) {
        if (NULL != (j = cJSON_CreateObject())) {
            cJSON_AddNumberToObject(j, "transactionId", x->transaction_id);
        }
    }
    return j;
}

char * cJSON_PrintRemoteStopTransactionReq(const struct RemoteStopTransactionReq * x) {
    char * s = NULL;
    if (NULL != x) {
        cJSON * j = cJSON_CreateRemoteStopTransactionReq(x);
        if (NULL != j) {
            s = cJSON_Print(j);
            cJSON_Delete(j);
        }
    }
    return s;
}

static void cJSON_DeleteRemoteStopTransactionReq(struct RemoteStopTransactionReq * x) {
    if (NULL != x) {
        cJSON_free(x);
    }
}

#ifdef __cplusplus
}
#endif

#endif /* __STDOUT__ */
